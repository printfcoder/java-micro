package mu.micro.java.framework.core.server;

import mu.micro.java.framework.core.metadata.Metadata;

import java.util.HashMap;

public class RpcServer {
    private static Server newRpcServer(Option... opts) {
       Options options = new Op
    }

    private static Options newOptions(Option... opts){
        Options options = new Options();
        options.setCodecs(new HashMap<>());
        options.setMetadata(new Metadata());
        options
    }
}
